# ---- 编译产物 ----------------------------------------------------------
# 图标生成器的 exe 可以从 make-icon.cs 重新编译，不入库
tools/make-icon.exe

# ---- 本地临时文件 ------------------------------------------------------
*.log
*.tmp
*.bak

# ---- 说明 --------------------------------------------------------------
# NetSwitcher-chs.exe / NetSwitcher-en.exe 是**故意入库**的：
# 这是给普通用户直接下载使用的 Windows 工具，仓库里带上编译好的 exe
# 才好用（大多数人机器上没有 C# 编译器，也不想为了改个 IP 去编译）。
# 如果你不想让二进制进仓库，把下面两行的注释去掉即可：
# NetSwitcher-chs.exe
# NetSwitcher-en.exe
